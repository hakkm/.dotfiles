super + c 
  gnome-control-center wifi

alt + space
  toggle_keyboard_layout

super + w 
  brave-beta 

Print
  file_path=~/pics/scrs/$(/bin/date +%y-%m-%d_%H:%M:%S).png && maim -s $file_path && sxiv $file_path

control + Print 
  file_path=~/pics/scrs/$(/bin/date +%y-%m-%d_%H:%M:%S).png && maim $file_path && sxiv $file_path

super + n
  emacs --file ~/t/rc.md

super + e 
  emacs ~/.emacs.org
